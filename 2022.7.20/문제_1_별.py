import turtle

myTurtle = turtle.Turtle()
for i in range(5):
    myTurtle.forward(100) #100픽셀 선 긋기
    myTurtle.right(144) # 우측으로 135도 돌리기
turtle.done()




# myTurtle.forward(130) #100픽셀 선 긋기
# # 돌릴때 우측이든 좌측이든 도형은 외각 이용해서 입력
# myTurtle.right(135) # 우측으로 135도 돌리기
# myTurtle.forward(140) # 140 픽셀 선 긋기
# myTurtle.right(145) # 우측으로 145도 돌리기
# myTurtle.forward(157) # 157 픽셀 선 긋기
# myTurtle.right(149) # 우측으로 149도 돌리기
# myTurtle.forward(155) # 150 픽셀 선 긋기
# myTurtle.right(149) # 우측으로 149도 돌리기